name: Medium Biomes
version: 1.1.0

pipeline:
  - src.plugins.noise

meta:
  base_version: "1.21.9"

data_pack:
  load: src/datapack
  description: {"text":"Medium Biomes\n1.21.9 - 26.2", "color":"gray"}
  min_format: 82
  max_format: 107

output: build