[project]
name = "medium-biomes"
version = "1.1.0"
description = "A datapack that adds medium sized biome generation."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beet>=0.115.0",
]
